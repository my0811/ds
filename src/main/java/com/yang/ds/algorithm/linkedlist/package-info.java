/**
 * 1.链表必须有头指针，或者头和尾的指针
 * 2.双端链表，是有两个指针，头指针，和尾指针
 * 3.双向链表是每个节点的指针方向是双向的，区别于双端链表
 * 4.双端链表可以两端插入，删除只能从一端删除,同时也只能从一端开始遍历
 * 5.双向链表，两端都可以插入和删除,可以从两端遍历，以为引用链式双向的
 * 6.双向链表可以直接当做队列使用，头部添加尾部删除
 */
package com.yang.ds.algorithm.linkedlist;